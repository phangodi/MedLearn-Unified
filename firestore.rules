rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // EMULATOR MODE: Allow all reads and writes for testing
    // TODO: Restore production rules before deploying to production
    match /{document=**} {
      allow read, write: if true;
    }

    /* PRODUCTION RULES - UNCOMMENT BEFORE DEPLOYING

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isAdmin() {
      return isSignedIn() &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }

    function isSuperAdmin() {
      return isSignedIn() &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'superadmin';
    }

    // Users collection
    match /users/{userId} {
      allow read: if true;
      allow create: if isSignedIn() && isOwner(userId);
      allow update: if isSignedIn() &&
                      isOwner(userId) &&
                      !request.resource.data.diff(resource.data).affectedKeys().hasAny(['isAdmin']);
      allow delete: if isAdmin();
    }

    // Posts collection
    match /posts/{postId} {
      allow read: if true;
      allow create: if isSignedIn();
      allow update: if isSignedIn() &&
                      (resource.data.author.id == request.auth.uid || isAdmin());
      allow delete: if isSignedIn() &&
                      (resource.data.author.id == request.auth.uid || isAdmin());
    }

    // Comments collection
    match /comments/{commentId} {
      allow read: if true;
      allow create: if isSignedIn();
      allow update, delete: if isSignedIn() &&
                              (resource.data.author.id == request.auth.uid || isAdmin());
    }

    // Announcements collection (notification system)
    match /announcements/{announcementId} {
      allow read: if isSignedIn();
      allow create, update, delete: if isSuperAdmin();
    }

    // Dashboard message document (notification system)
    match /dashboardMessage/current {
      allow read: if isSignedIn();
      allow write: if isSuperAdmin();
    }

    */
  }
}
